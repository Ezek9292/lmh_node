import userImage from '$lib/assets/user-profile.jpg';
import type { LayoutLoad } from './$types';

export const load: LayoutLoad = () => ({
	branchName: 'Lytup Sparklight Branch EGS',
	alarmCount: 2,
	userImage
});
